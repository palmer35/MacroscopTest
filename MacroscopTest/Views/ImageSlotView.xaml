<UserControl x:Class="MacroscopTest.Views.ImageSlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:MacroscopTest.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ImageSlotViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MacroscopTest;component/Views/Styles/ImageSlotResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Margin="10,4,10,12"
            Padding="12"
            Background="White"
            BorderBrush="#D0D7DE"
            BorderThickness="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            CornerRadius="8"
            MouseLeftButtonDown="OnContainerMouseLeftButtonDown">
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"
                               MinHeight="188" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    x:Name="ImageContainerBorder"
                    Margin="0,0,0,10"
                    Padding="10"
                    Background="#F5F5F5"
                    BorderBrush="#D0D7DE"
                    BorderThickness="1"
                    CornerRadius="6">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="#D0D7DE" />
                        <Setter Property="Background" Value="#F5F5F5" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#4F81BD" />
                                <Setter Property="BorderThickness" Value="2" />
                                <Setter Property="Background" Value="#DCEEFF" />
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="10"
                                                          ShadowDepth="0"
                                                          Opacity="0.35"
                                                          Color="#5C86B3" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Image x:Name="SlotImage"
                           Source="{Binding Image}"
                           Stretch="Uniform"
                           Cursor="Hand"
                           ToolTip="Double-click to open">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Opacity" Value="1" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>

                    <Button HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,0,4,0"
                            Style="{StaticResource ZoomOverlayButtonStyle}"
                            Click="OnOpenPreviewClick"
                            ToolTip="Open image" />

                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="#6B7280"
                               Text="IMAGE">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Image}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>

            <Grid Grid.Row="1"
                  Margin="0,0,0,12">
                <TextBox Style="{StaticResource RoundedTextBoxStyle}"
                         HorizontalContentAlignment="Left"
                         Text="{Binding Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#8A8F98"
                           IsHitTestVisible="False"
                           Text="URL">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Url}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Url}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <StackPanel Grid.Row="2"
                        Margin="0,0,0,8">
                <Button Margin="0,0,0,8"
                        Style="{StaticResource RoundedButtonStyle}"
                        Command="{Binding LoadCommand}"
                        Content="START" />

                <Button Style="{StaticResource RoundedButtonStyle}"
                        Command="{Binding CancelCommand}"
                        Content="STOP" />
            </StackPanel>

            <TextBlock Grid.Row="3"
                       Margin="0,0,0,4"
                       MinHeight="20"
                       HorizontalAlignment="Center"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Text="{Binding StatusText}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#6B7280" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusText}" Value="OK">
                                <Setter Property="Foreground" Value="#2E7D32" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding StatusText}" Value="Error">
                                <Setter Property="Foreground" Value="#C62828" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Grid.Row="4"
                       MinHeight="20"
                       Foreground="#C62828"
                       Text="{Binding ErrorText}"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
        </Grid>
    </Border>
</UserControl>
